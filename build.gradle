allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
    test {
        useJUnitPlatform()
    }
}

wrapper {
    gradleVersion = '6.5.1'
}